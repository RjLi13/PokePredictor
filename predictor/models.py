from django.db import models

# TODO better coding is to make Pokemon an model
# class Pokemon(models.Model):
#     name = models.CharField(max_length=50)
#     lvl = models.IntegerField(default=100)
#     max_HP = models.IntegerField(default=0)
#     current_HP = models.IntegerField(default=max_HP)
#
